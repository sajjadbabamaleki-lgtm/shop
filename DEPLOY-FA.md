# راهنمای انتشار روی لیارا

این راهنما برای کسی نوشته شده که برنامه‌نویس نیست. **هیچ جای آن ترمینال یا خط
فرمان لازم نیست** — همه‌چیز در مرورگر انجام می‌شود.

نسخهٔ فنی‌تر همین موضوع در `DEPLOY.md` است.

---

## این چطور کار می‌کند

یک فایل در مخزن گذاشته شده که به GitHub می‌گوید: «هر بار کد عوض شد، خودت
تست‌ها را اجرا کن و اگر سبز بودند، برنامه را روی لیارا منتشر کن.»

پس بعد از این‌که یک‌بار تنظیمات زیر را انجام دادید، **دیگر کاری لازم نیست**.
هر تغییری که در آینده روی کد انجام شود خودبه‌خود منتشر می‌شود.

کل کار سه بخش است و حدود ۱۵ دقیقه طول می‌کشد:

1. در لیارا: یک دیتابیس و یک برنامه بسازید
2. در GitHub: یک کلید مخفی اضافه کنید
3. در لیارا: کلید امنیتی برنامه را بسازید

---

## بخش ۱ — در پنل لیارا

وارد <https://console.liara.ir> شوید.

### ۱.۱ ساخت دیتابیس

فروشگاه بدون دیتابیس هیچ چیزی برای نشان دادن ندارد — دسته‌بندی‌ها، محصول‌ها،
قیمت‌ها و موجودی همه آنجا هستند.

1. از منوی سمت راست، **دیتابیس‌ها** را باز کنید
2. **ایجاد دیتابیس**
3. نوع: **PostgreSQL** — نسخهٔ ۱۶ یا بالاتر
4. یک پلن انتخاب کنید (کوچک‌ترین برای شروع کافی است)
5. بسازید و صبر کنید تا وضعیتش سبز شود

بعد روی دیتابیس کلیک کنید و صفحهٔ آن را **باز نگه دارید**. شش مقدار روی آن
هست که در قدم بعد لازم می‌شوند: `host`، `port`، `username`، `password` و نام
دیتابیس.

### ۱.۲ ساخت برنامه

1. از منو، **برنامه‌ها** → **ایجاد برنامه**
2. پلتفرم: **Laravel**
3. شناسه برنامه: **`vikyplus`**
   ⚠️ اگر اسم دیگری می‌گذارید، یادداشتش کنید — در بخش ۲ لازم می‌شود.
4. یک پلن انتخاب کنید و بسازید

### ۱.۳ تنظیم متغیرها

وارد برنامه شوید → تب **تنظیمات** → بخش **متغیرهای محیطی** (Environment
Variables). این‌ها را یکی‌یکی اضافه کنید:

| نام متغیر | مقدار |
|---|---|
| `APP_ENV` | `production` |
| `APP_DEBUG` | `false` |
| `APP_URL` | **آدرس دقیق برنامه**، مثلاً `https://vikyplus.liara.run` — این یکی از همه مهم‌تر است، پایین توضیح داده شده |
| `APP_LOCALE` | `fa` |
| `APP_TIMEZONE` | `Asia/Tehran` |
| `DB_CONNECTION` | `pgsql` |
| `DB_HOST` | از صفحهٔ دیتابیس |
| `DB_PORT` | از صفحهٔ دیتابیس |
| `DB_DATABASE` | از صفحهٔ دیتابیس |
| `DB_USERNAME` | از صفحهٔ دیتابیس |
| `DB_PASSWORD` | از صفحهٔ دیتابیس |
| `SESSION_DRIVER` | `database` |
| `CACHE_STORE` | `database` |
| `QUEUE_CONNECTION` | `database` |
| `LOG_CHANNEL` | `stderr` |

**ذخیره** کنید. `APP_KEY` عمداً در این لیست نیست — در بخش ۳ ساخته می‌شود.

### ⚠️ هر بار که متغیری را عوض کردید، این را هم بزنید

`liara_pre_start.sh` موقع هر دیپلوی `php artisan config:cache` را اجرا می‌کند،
یعنی مقدار همهٔ متغیرها در یک فایل PHP «پخته» می‌شود. اگر **بعد** از آن متغیری
را در پنل عوض کنید، برنامه همچنان مقدار قدیمی را می‌خواند و هیچ‌جا هم نمی‌گوید
چرا.

پس بعد از هر تغییر، در ترمینال برنامه:

```
php artisan config:cache
```

و برای اینکه ببینید برنامه واقعاً چه می‌بیند:

```
php artisan tinker --execute="echo config('services.sms.driver');"
```

این یک بار یک عصر وقت گرفت: متغیرها درست ست شده بودند و برنامه اصرار داشت
`SMS_DRIVER` هنوز `log` است. `php artisan sms:test` حالا خودش هشدار می‌دهد اگر
کانفیگ کش شده باشد.

### ⚠️ چرا `APP_URL` این‌قدر مهم است

فروشگاه حالا چند شعبه‌ای است. هر درخواستی که می‌رسد، اول باید بفهمد مال کدام
فروشگاه است — اصلی، یا شعبه‌ای مثل `vikyplus.ir/shiraz`. اگر آدرسی که برنامه
روی آن بالا می‌آید برایش ناشناخته باشد، **همهٔ صفحه‌ها ۴۰۴ می‌دهند**، نه فقط
یکی. سایت کاملاً خراب به نظر می‌رسد.

برای همین آدرس برنامه خودبه‌خود از `APP_URL` به‌عنوان آدرس فروشگاه اصلی ثبت
می‌شود. کافی است `APP_URL` دقیقاً همان آدرسی باشد که در مرورگر باز می‌کنید.

بعداً که دامنهٔ `vikyplus.ir` را به لیارا وصل کردید، `APP_URL` را همان کنید —
یا اگر می‌خواهید هر دو آدرس کار کنند، متغیر `CENTRAL_HOSTS` را با هر دو، جدا
شده با ویرگول، اضافه کنید:

```
CENTRAL_HOSTS=vikyplus.ir,www.vikyplus.ir,vikyplus.liara.run
```

---

## بخش ۲ — در GitHub

حالا باید به GitHub اجازه دهید به حساب لیارای شما وصل شود.

### ۲.۱ گرفتن توکن از لیارا

1. به <https://console.liara.ir/API> بروید
2. یک توکن بسازید و **کپی** کنید

این توکن مثل رمز عبور است. جایی منتشرش نکنید.

### ۲.۲ گذاشتن توکن در GitHub

1. مخزن `sajjadbabamaleki-lgtm/shop` را در GitHub باز کنید
2. **Settings** (تنظیمات، بالای صفحه)
3. از منوی سمت چپ: **Secrets and variables** → **Actions**
4. دکمهٔ سبز **New repository secret**
5. نام: **`LIARA_API_TOKEN`** — دقیقاً همین، با حروف بزرگ
6. مقدار: توکنی که کپی کردید
7. **Add secret**

### ۲.۳ فقط اگر اسم برنامه را چیز دیگری گذاشتید

اگر در بخش ۱.۲ اسمی غیر از `vikyplus` انتخاب کردید:

1. در همان صفحه، تب **Variables** (کنار Secrets)
2. **New repository variable**
3. نام: `LIARA_APP` — مقدار: اسمی که انتخاب کردید

اگر اسم را `vikyplus` گذاشتید، این قدم را رد کنید.

---

## بخش ۳ — کلید امنیتی برنامه

لاراول یک کلید می‌خواهد تا کوکی‌ها را رمز کند. بدون آن هر صفحه خطا می‌دهد.
این کلید باید یک‌بار ساخته شود و بعد ثابت بماند.

**اول باید یک‌بار برنامه منتشر شود** تا کنسولش در دسترس بیاید:

1. در GitHub، تب **Actions** را باز کنید
2. آخرین اجرا را ببینید. اگر منتظر است یا در حال اجراست، صبر کنید
3. اگر اجرایی نبود، از منوی چپ **Tests and deploy** → دکمهٔ **Run workflow**

وقتی انتشار تمام شد:

1. در پنل لیارا وارد برنامه شوید → تب **کنسول**
2. این را تایپ کنید و Enter بزنید:

   ```
   php artisan key:generate --show
   ```

3. چیزی شبیه `base64:xxxxxxxxxxxxx...` چاپ می‌شود. کاملش را کپی کنید
4. برگردید به **تنظیمات** → **متغیرهای محیطی**
5. متغیر جدید: نام `APP_KEY` — مقدار همان چیزی که کپی کردید
6. ذخیره کنید؛ برنامه خودش دوباره بالا می‌آید

حالا آدرس برنامه را باز کنید. فروشگاه باید کامل بالا بیاید.

---

## از این به بعد

هر تغییری که در کد داده شود، خودبه‌خود منتشر می‌شود. در تب **Actions** در
GitHub می‌توانید ببینید هر انتشار موفق بوده یا نه:

- ✅ سبز — منتشر شد
- ❌ قرمز — چیزی خراب بوده و **منتشر نشده**. نسخهٔ قبلی سالم روی سایت می‌ماند

اگر تست‌ها رد شوند، انتشار اصلاً انجام نمی‌شود. این عمدی است: یک نسخهٔ خراب
هیچ‌وقت روی سایت نمی‌رود.

---

## اگر خطا خوردید

**اولین اجرا قرمز شد** — طبیعی است، اگر هنوز بخش ۲ را انجام نداده باشید.
توکن را اضافه کنید و در تب Actions دکمهٔ **Re-run jobs** را بزنید.

**همهٔ صفحه‌ها ۴۰۴ می‌دهند** — تقریباً همیشه یعنی `APP_URL` با آدرس واقعی
سایت یکی نیست، پس برنامه آدرس خودش را نمی‌شناسد. درستش کنید و یک‌بار دیگر
منتشر کنید. راه دوم: `CENTRAL_HOSTS` را با آدرس درست اضافه کنید.

**سایت بالا آمد ولی صفحه به‌هم‌ریخته و بدون رنگ و عکس است** — این هم
`APP_URL` است. درستش کنید.

**هر صفحه خطای ۵۰۰ می‌دهد** — احتمالاً `APP_KEY` را نگذاشته‌اید. بخش ۳.
در لاگ برنامه در پنل لیارا هم پیام واضحی دربارهٔ همین چاپ می‌شود.

**«could not connect to database»** — یکی از پنج مقدار `DB_` اشتباه است.
از صفحهٔ دیتابیس دوباره کپی کنید.

در هر حالت دیگری، متن خطا را از تب **Actions** در GitHub یا از **لاگ** برنامه
در لیارا کپی کنید و بفرستید.

---

## درگاه پرداخت — زرین‌پال

کد درگاه نوشته و تست شده است. روشن کردنش **دو متغیر** است و هیچ تغییری در کد
لازم ندارد.

| نام متغیر | مقدار |
|---|---|
| `PAYMENT_DRIVER` | `zarinpal` |
| `ZARINPAL_MERCHANT_ID` | همان کد ۳۶ کاراکتری با خط تیره، از پنل زرین‌پال |

بعدش، مثل هر متغیر دیگری، در کنسول برنامه:

```
php artisan config:cache
```

### دو دامنه — `vikyplus.ir` و `www.vikyplus.ir`

آدرس بازگشت از درگاه، **همان دامنه‌ای است که خریدار روی آن خرید کرده**؛ برنامه
آن را از روی آدرس همان درخواست می‌سازد، نه از یک آدرس ثابت. یعنی کسی که از
`www.vikyplus.ir` وارد شده، به `www.vikyplus.ir` برمی‌گردد و کسی که از
`vikyplus.ir` آمده، به `vikyplus.ir`.

پس **هر دو دامنه باید در پنل زرین‌پال روی درگاه ثبت و تأیید شده باشند.** اگر
فقط یکی ثبت شود، خریدارهایی که از آن یکی آمده‌اند، سر «پرداخت» پیغام
«درگاه پرداخت درخواست را نپذیرفت» می‌گیرند — و این فقط برای نصف خریدارها اتفاق
می‌افتد، که همان چیزی است که دیر پیدا می‌شود.

هر دو دامنه باید در لیارا هم به برنامه وصل باشند و در `CENTRAL_HOSTS` هر دو
نوشته شده باشد (پیش‌فرض برنامه هر دو را دارد):

```
CENTRAL_HOSTS=vikyplus.ir,www.vikyplus.ir,vikyplus.liara.run
```

اگر دامنه‌ای در این لیست نباشد، آن آدرس **همهٔ صفحه‌هایش ۴۰۴** می‌شود، نه فقط
پرداختش.

### `ZARINPAL_SANDBOX` — فقط برای تمرین

با `ZARINPAL_SANDBOX=true` همه‌چیز به سرور آزمایشی زرین‌پال می‌رود و پرداخت‌ها
واقعی نیستند. **روی سایت اصلی هیچ‌وقت `true` نماند** — سفارش‌هایی «پرداخت‌شده»
ثبت می‌شوند که پولی برایشان نیامده. سرور آزمایشی جای دیگری است، پس آزمایش
نهایی به‌هرحال یک خرید واقعیِ کوچک لازم دارد.

### وقتی روشن شد، چه چیزی روی سایت عوض می‌شود

- صفحهٔ سفارش به‌جای «پرداخت هنگام تحویل انجام می‌شود»، دکمهٔ پرداخت نشان
  می‌دهد.
- سفارش‌های تازه در پنل «پرداخت اینترنتی» ثبت می‌شوند، نه «پرداخت در محل» —
  یعنی کسی که سفارش را می‌بندد می‌داند نباید از پیک پول بگیرد. سفارش‌های قدیمی
  دست‌نخورده می‌مانند.
- پرداخت فقط وقتی «انجام شد» ثبت می‌شود که خود زرین‌پال، سرور به سرور، تأییدش
  کند. برگشتن با آدرس `Status=OK` به‌تنهایی هیچ سفارشی را پرداخت‌شده نمی‌کند.

### اگر درگاه درخواست را نپذیرفت

اگر سر پرداخت پیغام «درگاه پرداخت درخواست را نپذیرفت» آمد، در کنسول برنامه:

```
php artisan payment:test
```

یک درخواست واقعی به زرین‌پال می‌فرستد (بدون اینکه پولی جابه‌جا شود یا چیزی در
دیتابیس ثبت شود) و می‌گوید کجای کار می‌لنگد: کد درگاه، آدرسی که با آن حرف زده،
آدرس بازگشت، IP این سرور، و پاسخ خود زرین‌پال با کد خطایش. خروجی‌اش را می‌شود
همان‌طور که هست برای پشتیبانی زرین‌پال فرستاد.

### اگر پرداختی گم شد

هر تلاش پرداخت — موفق یا ناموفق — یک ردیف در جدول `payments` است، با شمارهٔ
پیگیری زرین‌پال، چهار رقم آخر کارت، و اگر رد شده، دلیلش به زبان خود درگاه.
شمارهٔ پیگیری در صفحهٔ سفارش خود مشتری هم چاپ می‌شود.

## شعبه‌ها

بعد از اینکه سایت بالا آمد، هر شعبه یک ردیف در دیتابیس است و **هیچ دامنهٔ
جدیدی لازم ندارد**. در پنل لیارا، در کنسول برنامه:

```
php artisan tinker
```

و بعد:

```php
App\Models\Branch::create([
    'slug' => 'shiraz',
    'name' => 'ویکی پلاس شیراز',
    'type' => 'franchise',
    'city' => 'شیراز',
]);
```

از همان لحظه `آدرس-سایت/shiraz` باز می‌شود. برای «ویکی پلاس محسن» هم
`slug` را `mohsen` بگذارید.

فعلاً هر شعبه همان محصولات و قیمت‌های فروشگاه اصلی را نشان می‌دهد؛ قیمت و
موجودی مخصوص هر شعبه کار فاز بعدی است. پنل مدیریتی برای ساختن شعبه هم بعداً
می‌آید — تا آن موقع همین دستور کار را راه می‌اندازد.

## دربارهٔ لینک قدیمی

لینک Netlify فعلاً دست‌نخورده می‌ماند و همان صفحهٔ نمایشی را نشان می‌دهد. تا
وقتی آدرس جدید لیارا سالم بالا نیامده، آن را حذف نکنید. بعد از آن می‌شود
`vikyplus.ir` را به لیارا وصل کرد.

## لاگ‌ها کجا هستند؟

از این به بعد در production دو جا نوشته می‌شوند:

- **لاگ‌ویوئر پنل لیارا** — چون برنامه روی `stderr` هم می‌نویسد و لیارا آن را جمع می‌کند. این نسخه با دیپلوی بعدی پاک **نمی‌شود**.
- **فایل روزانه** داخل کانتینر، در `storage/logs/laravel-YYYY-MM-DD.log`، با نگهداری ۱۴ روز. از کنسول اپ در پنل لیارا خواندنی است، ولی با دیپلوی بعدی می‌رود.

سطح لاگ در production روی `warning` است، نه `debug`. یعنی فقط چیزی نوشته می‌شود که کسی باید کاری درباره‌اش بکند.

**هیچ متغیری برای این‌ها لازم نیست.** اگر `LOG_CHANNEL` را در لیارا ست کنید، همین را خاموش می‌کنید — ستش نکنید.

## دیسک — جایی که عکس‌ها می‌مانند

کانتینر با هر دیپلوی از نو ساخته می‌شود. یعنی هر فایلی که برنامه نوشته باشد —
عکسی که در پنل ادمین آپلود شده، عکسی که `basalam:fetch` دانلود کرده — با
دیپلوی بعدی نیست. این نه خطاست و نه هشدار می‌دهد: فایل‌ها می‌روند و در سایت
جایشان عکس شکسته می‌ماند.

راهش یک دیسک است که در پنل لیارا ساخته می‌شود و به مسیر `storage/app` سوار
می‌شود. mount در `liara.json` نوشته شده:

```json
"disks": [
    { "name": "viikyplus", "mountTo": "storage/app" }
]
```

`name` باید **دقیقاً** شناسه‌ای باشد که موقع ساخت دیسک در پنل نوشتید، و دیسک
باید زیر همین اپ ساخته شده باشد نه اپ دیگری. اگر فرق داشته باشد لیارا دیپلوی
را رد می‌کند — `Error: CODE 400: Disk ... does not exist` — که خوب است، چون
جلوی رفتن نسخه‌ای که عکس‌هایش جایی برای ماندن ندارند را از همان اول می‌گیرد.
یک بار همین‌جا اتفاق افتاد: اسم `viikyplus` بود و از روی عکس `vikyplus`
خوانده شده بود.

**بعد از دیپلوی، یک بار چک کنید** که واقعاً سوار شده. در کنسول اپ:

```
df -h /var/www/html/storage/app
```

اگر خط جوابش فایل‌سیستمی جدا با اندازهٔ دیسک شما نشان داد، سوار است. اگر همان
فایل‌سیستم ریشه (`overlay`) بود، سوار نشده و `name` را باید درست کرد.

دو نکتهٔ دیگر که `liara_pre_start.sh` خودش انجام می‌دهد و لازم نیست کاری کنید:

- **دیسک تازه خالی است** و وقتی روی `storage/app` سوار شود، `storage/app/public`
  و `storage/app/private` را که در ایمیج بودند می‌پوشاند. اسکریپت هر دو را قبل
  از هر چیز دیگری می‌سازد.
- **`storage:link`** لینک `public/storage` را می‌سازد. بدون آن فایل روی دیسک
  هست و از صفحه دیده نمی‌شود.
